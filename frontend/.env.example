NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Development only. Do not enable for final submission unless you intentionally
# want to run the frontend without the real backend.
# NEXT_PUBLIC_ENABLE_MOCK_API=true
